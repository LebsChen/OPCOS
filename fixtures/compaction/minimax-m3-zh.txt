**Goal**
修复定价模块 `src/pricing.py` 中的舍入 bug（负数金额下的银行家舍入问题），并补充回归测试。

**Completed actions and results**
- 读取 `src/pricing.py`，定位原逻辑 `return round(amount * 1.08)`（Python 内建 `round` 使用银行家舍入，会导致一半值时偏向偶数）。
- 对 `src/pricing.py` 进行编辑，将返回值改为 `float(Decimal(amount * 1.08).quantize(Decimal('0.01'), rounding=ROUND_HALF_UP))`。
- 执行 `pytest tests/test_pricing.py`：3 通过，1 失败 —— `test_negative_amount` 期望 `-1.08`，实际 `-1.07`。

**Key discoveries and file paths**
- `src/pricing.py`：bug 根因未完全消除 —— `amount * 1.08` 仍是浮点乘法，`-1.0 * 1.08` 实际值为 `-1.0799999999999998`；只有在乘**之前**先把 `amount` 转为 `Decimal`，再乘以 `Decimal('1.08')`，再 `quantize(..., ROUND_HALF_UP)`，才能得到 `-1.08`。
- 编辑后文件中尚未确认 `from decimal import Decimal, ROUND_HALF_UP` 已被加上（若缺失需补齐导入）。
- `tests/test_pricing.py` 已存在，包含 `test_negative_amount` 等用例；用户要求“补充回归测试”尚未执行。

**Unfinished next steps**
- 修正 `src/pricing.py` 的实现：将入参先转 `Decimal`，再用 `Decimal('1.08')` 相乘并 `quantize(Decimal('0.01'), rounding=ROUND_HALF_UP)`，并补齐 `Decimal` / `ROUND_HALF_UP` 导入。
- 在 `tests/test_pricing.py` 中补充回归测试用例（如负数半值 `amount=-1.0` 期望 `-1.08`、边界值等）以覆盖并固化修复行为。
- 重新运行 `pytest tests/test_pricing.py`，确认全部用例通过。